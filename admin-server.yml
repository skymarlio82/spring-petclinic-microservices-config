server:
  port: 9090
spring:
  security:
    user:
      name: "admin"
      password: "password"
  mail:
    default-encoding: UTF-8
    host: smtp.office365.com
    port: 587
    username: S116433@ap.merckgroup.com
    password: q@d4MwCZnu
    from: bioinfo@merckgroup.com
    properties:
      mail:
        smtp:
          auth: true
          timeout: 25000
          starttls:
            enable: true
            #required: true
  boot.admin.notify.mail.to: jitao.liu@external.merckgroup.com
eureka:
  instance:
    instance-id: ${spring.application.name}:${server.port}
    leaseRenewalIntervalInSeconds: 10
    health-check-url-path: /actuator/health
    metadata-map:
      user.name: ${spring.security.user.name}
      user.password: ${spring.security.user.password}
  client:
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
